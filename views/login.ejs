<!DOCTYPE html>
<html lang="hu">

<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="style.css">

    <title>Vonattársaság</title>
</head>

<body>
    <div class="container mt-4">
        <div class="mb-3 text-center">
            <h1>Bejelentkezés</h1>
            <br>
        </div>
    </div>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="container mt-4">
                    <% if (query && query==='success' ) { %>
                        <div class="alert alert-success" role="alert">
                            Sikeres Regisztráció!
                        </div>
                        <% } else if (query && query==='error' ) { %>
                            <div class="alert alert-danger" role="alert">
                                Sikertelen bejelentkezés! <%= message %>
                            </div>
                            <% } %>
                                <form method="POST" action="/login">
                                    <div class="mb-3">
                                        <label for="felhasznalonev" class="form-label">Felhasználónév:</label>
                                        <input type="text" name="felhasznalonev" class="form-control"
                                            id="felhasznalonev" placeholder="Add meg a felhasználónevet!" required>
                                    </div>

                                    <div class="mb-3">
                                        <label for="jelszo" class="form-label">Jelszó:</label>
                                        <input type="password" name="jelszo" class="form-control" id="jelszo"
                                            placeholder="Add meg a jelszót!" required>
                                    </div>

                                    <div class="mb-3 d-grid">
                                        <input type="submit" class="btn btn-primary" value="Belépés">
                                    </div>

                                </form>

                                <div class="text-center">
                                    <span class="text-muted">Nincs még fiókod? <a href="/regisztracio"
                                            class="text-decoration-none text-primary">Regisztrálj itt.</a></span>
                                </div>
                                <div class="text-center">
                                    <span class="text-muted">Vendégként való <a href="/"
                                            class="text-decoration-none text-primary">belépés</a>.</span>
                                </div>
                </div>
            </div>
        </div>


</body>

</html>