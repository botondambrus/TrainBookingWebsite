<!DOCTYPE html>
<html lang="hu">

<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="style.css">

    <title>Vonattársaság</title>
</head>

<body>
    <div class="container mt-4">
        <div class="mb-3 text-center">
            <h1>Regisztráció</h1>
            <br>
        </div>
    </div>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <% if (query && query==='error' ) { %>
                    <div class="alert alert-danger" role="alert">
                        Sikertelen Regisztráció! <%= message %>
                    </div>
                    <% } %>
                        <form method="POST" action="/regisztracio">
                            <div class="mb-3">
                                <label for="felhasznalonev" class="form-label">Felhasználónév:</label>
                                <input type="text" name="felhasznalonev" class="form-control" id="felhasznalonev"
                                    placeholder="Add meg a felhasználónevet!" required>
                            </div>

                            <div class="mb-3">
                                <label for="nev" class="form-label">Név:</label>
                                <input type="text" name="nev" class="form-control" id="nev"
                                    placeholder="Add meg a nevet!" required>
                            </div>

                            <div class="mb-3">
                                <label for="email" class="form-label">E-mail cim:</label>
                                <input type="text" name="email" class="form-control" id="email"
                                    placeholder="Add meg az e-mail cimet!" required>
                            </div>

                            <div class="mb-3">
                                <label for="jelszo" class="form-label">Jelszó:</label>
                                <input type="password" name="jelszo" class="form-control" id="jelszo"
                                    placeholder="Add meg a jelszót!" required>
                            </div>

                            <div class="mb-3">
                                <label for="jelszoismetles" class="form-label">Jelszó ismétlése:</label>
                                <input type="password" name="jelszoismetles" class="form-control" id="jelszoismetles"
                                    placeholder="Ismételd meg a jelszót!" required>
                            </div>

                            <div class="mb-3 d-grid">
                                <input type="submit" class="btn btn-primary" value="Regisztráció">
                            </div>
                        </form>

                        <div class="text-center">
                            <span class="text-muted">Van már fiókod? <a href="/login"
                                    class="text-decoration-none text-primary">Lépj be itt</a></span>
                        </div>
            </div>
        </div>
    </div>