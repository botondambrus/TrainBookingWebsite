<!DOCTYPE html>
<html lang="hu">

<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Vonattársaság</title>
</head>

<body>
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link " href="/">Keresés</a>
                </li>
                <% if ( role==='admin' ) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/hozzaadas">Hozzáadás</a>
                    </li>
                    <% } %>
                        <% if (role !=='vendeg' ) { %>
                            <li class="nav-item">
                                <a class="nav-link active" href="/foglalas/user">Foglalásaim</a>
                            </li>
                            <% } %>
            </ul>
            <ul class="navbar-nav ms-auto">
                <% if (role==='vendeg' ) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Bejelentkezés</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/regisztracio">Regisztráció</a>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link">
                                Felhasználó: <%= felhasznalonev %>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/login/logout">Kijelentkezés</a>
                        </li>
                        <% } %>
            </ul>
        </div>
    </nav>
    <div>
        <div>
            <div class="mt-5 text-center">
                <h3>Foglalás a(z) <%= jaratId %> számú járatra</h3>
            </div>
        </div>
        <div class="container" id="adatok">
            <form method="POST" action="/foglalas/jarat/<%= jaratId%>">
                <div class="mb-3">
                    <label for="datum" class="form-label">Dátum:</label>
                    <input type="date" class="form-control" id="datum" name="datum" required>
                </div>
                <div class="mb-3">
                    <input type="submit" class="btn btn-primary" value="Foglalás">
                </div>
            </form>
        </div>
    </div>
</body>

</html>